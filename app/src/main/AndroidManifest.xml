<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProjectJTCM">

        <activity
            android:name=".ui.onboarding.RoleSelectionActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".ui.auth.RegisterActivity"
            android:exported="false" />

        <activity android:name=".ui.user.MainActivity" android:exported="false" />
        <activity android:name=".ui.user.OrderPickupActivity" android:exported="false" />
        <activity android:name=".ui.user.HistoryActivity" android:exported="false" />
        <activity android:name=".ui.user.PriceListActivity" android:exported="false" />
        <activity android:name=".ui.user.ProfileActivity" android:exported="false" />
        <activity android:name=".ui.user.QrDisplayActivity" android:exported="false" />

        <activity android:name=".ui.collector.CollectorMainActivity" android:exported="false" />
        <activity android:name=".ui.collector.CollectorPriceListActivity" android:exported="false" />

    </application>

</manifest>